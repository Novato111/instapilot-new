/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "scontent.cdninstagram.com",
      "instagram.fbom23-1.fna.fbcdn.net", // Add this domain here
    ],
  },
};

export default nextConfig;
